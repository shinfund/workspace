# Claude 설정
.claude/

# API 토큰 (절대 커밋 금지)
scripts/kis_token.json
Usersshinfworkspacescriptskis_token.json

# 의존성
node_modules/

# 원시 데이터 (바이너리·대용량 제외, HTML 리포트는 추적)
data/**/*.xlsx
data/**/*.hwp
data/**/*.hwpx

# work-portal·stock-portal은 별도 중첩 git 저장소(각자 own .git) — 워크스페이스 저장소에서 제외
apps/work-portal/
apps/stock-portal/

# PyInstaller 빌드 산출물 (exe는 로컬에서 재빌드)
apps/image-compressor/build/
apps/image-compressor/dist/
apps/**/__pycache__/

# 로그·임시 파일
scripts/report_*.txt
scripts/krx_temp.json
scripts/krx_monitor_state.txt
scripts/krx_monitor_log.txt
scripts/lotto_update_log.txt
scripts/_tmp_*
*.log
.tmp.driveupload
*.tmp
master_only_test.xlsx
xtest_extract/

# 캐시·원시조회 데이터(재생성 가능, 소스 아님)
data/괴리율/top50_cache.json
data/전압분석/*_raw.json

# 자동 아카이브(Archive_DailyCleanup 예약작업이 관리하는 롤링 버퍼, 수동 추적 불필요)
data/analysis/archive/

# 브라우저 중복다운로드 사본 ("파일명 (1).html" 등)
* (*).html
* (*).xlsx
